fn main(){
    let mut x = 1000;
    let y = "Programming";
    println!("x:{}\ny:{}", x, y);
    x = 1100;
    println!("x:{}\ny:{}", x, y);
}

// course
/* fn main2(){
    // declare a mutable variable `x`
    let mut x = 1000;
    // declare a variable `y`
    let y="Programming";
    // print output of `x`
    println!("x:{}", x);
    // print output of `y`
    println!("y:{}", y);
    // update x
    x = 1100;
    // print output of `x`
    println!("x:{}", x);
    // print output of `y`
    println!("y:{}", y);
} */
